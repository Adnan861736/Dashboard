# 📘 دليل الإعداد والتشغيل - منصة تعزيز الوعي المجتمعي

## ✅ المتطلبات الأساسية

قبل البدء، تأكد من توفر:
- **Node.js** الإصدار 18 أو أحدث
- **npm** أو **yarn**
- **Backend API** يعمل على المنفذ 5000

---

## 🚀 خطوات التثبيت السريعة

### 1. تثبيت المكتبات
```bash
npm install
```

### 2. إعداد المتغيرات البيئية
تأكد من وجود ملف `.env.local` بالمحتوى التالي:
```env
NEXT_PUBLIC_API_URL=http://localhost:5000
```

### 3. تشغيل المشروع
```bash
npm run dev
```

### 4. فتح المتصفح
```
http://localhost:3000
```

---

## 🔐 بيانات تسجيل الدخول التجريبية

للتجربة، استخدم:
- **البريد الإلكتروني**: admin@example.com
- **كلمة المرور**: 123456

*(ملاحظة: هذه البيانات يجب أن تكون موجودة في الـ Backend)*

---

## 📂 الصفحات المتاحة

| المسار | الوصف |
|--------|-------|
| `/` | توجيه تلقائي لصفحة تسجيل الدخول |
| `/login` | صفحة تسجيل الدخول |
| `/dashboard` | لوحة التحكم الرئيسية |
| `/dashboard/users` | إدارة المستخدمين |
| `/dashboard/articles` | إدارة المقالات |
| `/dashboard/surveys` | إدارة الاستبيانات |

---

## 🎨 الميزات المطبقة

### ✅ التصميم والواجهة
- [x] نظام ألوان مخصص لبناء السلام
- [x] Dark Mode كامل
- [x] دعم اللغة العربية (RTL)
- [x] دعم اللغة الإنجليزية (LTR)
- [x] تصميم متجاوب على جميع الشاشات
- [x] Atomic Design Pattern

### ✅ الأمان
- [x] تسجيل دخول آمن مع JWT
- [x] Middleware لحماية المسارات
- [x] صلاحيات المستخدمين (Admin/User)
- [x] Auto-logout عند انتهاء الجلسة
- [x] حماية جميع صفحات Dashboard

### ✅ لوحة التحكم
- [x] إحصائيات شاملة (Users, Articles, Sessions, Points)
- [x] رسوم بيانية تفاعلية (Bar Chart, Line Chart)
- [x] Sidebar قابل للطي على الموبايل
- [x] Header مع تبديل Dark Mode واللغة

### ✅ CRUD Operations

#### المستخدمون
- [x] عرض قائمة المستخدمين
- [x] البحث والتصفية
- [x] عرض النقاط والصلاحيات
- [x] حذف مستخدم (Admin فقط)

#### المقالات
- [x] إنشاء مقال جديد
- [x] تعديل مقال موجود
- [x] حذف مقال
- [x] ربط التصنيفات
- [x] بحث في المقالات

#### الاستبيانات
- [x] إنشاء استبيان متعدد الأسئلة
- [x] إضافة خيارات متعددة لكل سؤال
- [x] تحديد الإجابات الصحيحة
- [x] ربط الاستبيان بمقال
- [x] عرض الاستبيانات

---

## 🔌 API Endpoints المستخدمة

جميع الـ endpoints موجودة في: `lib/api.ts`

### Authentication
```javascript
POST /api/auth/login          // تسجيل الدخول
POST /api/auth/register       // تسجيل مستخدم جديد
GET  /api/auth/profile        // الملف الشخصي
```

### Users
```javascript
GET    /api/users             // جميع المستخدمين
GET    /api/users/:id         // مستخدم معين
PUT    /api/users/:id         // تحديث مستخدم
DELETE /api/users/:id         // حذف مستخدم
GET    /api/users/leaderboard // لوحة الصدارة
```

### Articles
```javascript
GET    /api/articles          // جميع المقالات
GET    /api/articles/:id      // مقال معين
POST   /api/articles          // إنشاء مقال
PUT    /api/articles/:id      // تحديث مقال
DELETE /api/articles/:id      // حذف مقال
```

### Surveys
```javascript
POST /api/surveys                     // إنشاء استبيان
GET  /api/surveys/article/:articleId  // استبيان حسب المقال
POST /api/surveys/:id/submit          // إرسال إجابات
GET  /api/surveys/:id/results         // نتائج الاستبيان
```

---

## 🛠 استكشاف الأخطاء الشائعة

### ❌ خطأ: Cannot connect to backend
**السبب**: الـ Backend API غير متصل

**الحل**:
```bash
# تأكد من تشغيل الـ Backend على المنفذ 5000
# أو غيّر NEXT_PUBLIC_API_URL في .env.local
```

### ❌ خطأ: Unauthorized 401
**السبب**: Token غير صالح أو منتهي

**الحل**:
```bash
# امسح localStorage
localStorage.clear()
# ثم سجّل الدخول مرة أخرى
```

### ❌ خطأ: Module not found
**السبب**: مكتبة مفقودة

**الحل**:
```bash
npm install
```

### ❌ خطأ: i18n translations not working
**السبب**: ملفات الترجمة مفقودة

**الحل**:
```bash
# تأكد من وجود:
# messages/ar.json
# messages/en.json
```

---

## 🔧 التخصيص

### تغيير الألوان
افتح `app/globals.css` وعدّل المتغيرات:
```css
:root {
  --primary: 209 78% 58%; /* غيّر هنا */
}
```

### إضافة صفحة جديدة
```bash
# 1. أنشئ مجلد جديد
mkdir app/dashboard/new-page

# 2. أنشئ ملف page.tsx
touch app/dashboard/new-page/page.tsx

# 3. أضف الصفحة إلى Sidebar
# افتح components/organisms/Sidebar.tsx
```

### إضافة ترجمة جديدة
```json
// messages/ar.json
{
  "myNewSection": {
    "title": "العنوان الجديد"
  }
}

// messages/en.json
{
  "myNewSection": {
    "title": "New Title"
  }
}
```

---

## 📦 البناء للإنتاج

```bash
# بناء المشروع
npm run build

# تشغيل النسخة المبنية
npm start
```

---

## 🗂 هيكل المشروع الكامل

```
my-next-app/
├── app/
│   ├── (auth)/
│   │   └── login/page.tsx          # صفحة تسجيل الدخول
│   ├── dashboard/
│   │   ├── page.tsx                # Dashboard الرئيسية
│   │   ├── users/page.tsx          # إدارة المستخدمين
│   │   ├── articles/page.tsx       # إدارة المقالات
│   │   ├── surveys/page.tsx        # إدارة الاستبيانات
│   │   └── layout.tsx              # Layout الخاص بالـ Dashboard
│   ├── globals.css                 # الأنماط العامة + نظام الألوان
│   ├── layout.tsx                  # Root Layout
│   └── page.tsx                    # الصفحة الرئيسية (توجيه)
├── components/
│   ├── atoms/                      # مكونات أساسية
│   │   ├── Button.tsx              # زر قابل لإعادة الاستخدام
│   │   ├── Card.tsx                # بطاقة
│   │   ├── Input.tsx               # حقل إدخال
│   │   └── LoadingSpinner.tsx      # شاشة التحميل
│   ├── molecules/                  # مكونات مركبة
│   │   └── StatCard.tsx            # بطاقة إحصائيات
│   ├── organisms/                  # مكونات معقدة
│   │   ├── Sidebar.tsx             # القائمة الجانبية
│   │   └── Header.tsx              # الهيدر العلوي
│   └── providers/                  # Context Providers
│       ├── ThemeProvider.tsx       # Dark Mode Provider
│       └── ToastProvider.tsx       # Notifications Provider
├── lib/
│   ├── api.ts                      # جميع API Endpoints
│   ├── auth-context.tsx            # Authentication Context
│   └── utils.ts                    # Utility Functions
├── messages/
│   ├── ar.json                     # الترجمة العربية
│   └── en.json                     # الترجمة الإنجليزية
├── middleware.ts                   # حماية المسارات
├── i18n.ts                         # إعدادات i18n
├── .env.local                      # المتغيرات البيئية
├── tailwind.config.js              # إعدادات Tailwind
├── next.config.ts                  # إعدادات Next.js
├── package.json                    # المكتبات
└── README.md                       # التوثيق
```

---

## 🌟 نصائح للتطوير

1. **استخدم TypeScript**: جميع الملفات مكتوبة بـ TypeScript للأمان
2. **اتبع Atomic Design**: ضع المكونات في المجلد المناسب
3. **استخدم Toast للإشعارات**: `toast.success()` أو `toast.error()`
4. **اختبر Dark Mode**: تأكد من أن التصميم يعمل في الوضعين
5. **اختبر RTL/LTR**: تأكد من دعم اللغتين

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع هذا الدليل
2. تحقق من `lib/api.ts` للـ endpoints
3. تحقق من Console للأخطاء
4. تأكد من تشغيل الـ Backend

---

## 🎉 جاهز للاستخدام!

المشروع الآن جاهز للتطوير والاستخدام. استمتع ببناء منصة تعزيز الوعي المجتمعي! 🚀

---

**آخر تحديث**: نوفمبر 2025
